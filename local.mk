# can use make/shell vars defined elsewhere e.g. ${HOME}
GSL = <dir holding include and lib dirs of where the GNU scientific lib is installed>
SAMTOOL = <main samtools dir>
KENT = <when upzipping UCSC code this is the src dir i.e. kent/src>
